// Настройки подобраны под уже написанный код, а не наоборот: цель — чтобы
// внедрение форматтера не переписало стиль проекта. Замеры по src на момент
// внедрения: одинарные кавычки в 289 импортах против 3 двойных, точка с запятой
// в конце строки, отступ в 2 пробела.
/** @type {import('prettier').Config} */
export default {
  singleQuote: true,
  semi: true,
  tabWidth: 2,

  // Шире дефолтных 80: в коде 260 строк длиннее сотни — в основном JSX с
  // классами и русские комментарии. На 80 форматтер рвал бы их на лоскуты.
  printWidth: 100,

  // Прод-код и так писался с висячими запятыми; для diff'ов это лучше —
  // добавление поля не трогает предыдущую строку.
  trailingComma: 'all',

  // JSX-атрибуты в двойных кавычках — как в HTML и как уже написано в разметке.
  jsxSingleQuote: false,
};

// Карта Яндекс.Конструктора. Метки и подписи заданы НЕ здесь, а в кабинете Яндекса:
// карту опознаёт идентификатор um=constructor:<хэш> внутри ссылки. Ключ API не нужен,
// поэтому нет ни привязки к домену, ни квот — на этом как раз погорела карта 2ГИС
// (тайлы начали отдавать 403 с чужим ключом, подложка исчезла).
//
// ⚠️ Обратная сторона: метки правятся в кабинете, а не в коде. Аккаунт Яндекса,
// на котором собрана карта, нужен рабочий — с личного её потом не отредактировать.
//
// Как обновить ссылку: Конструктор карт → нужная карта → «Код карты» → вкладка
// с iframe → скопировать оттуда значение src целиком.
//
// Ссылка взята из Конструктора как есть, своих параметров не добавляем. Размеры он
// отдаёт атрибутами iframe (width=968 height=420) — их не переносим: ширину задаёт
// CSS, иначе карта перестанет быть резиновой и на телефоне вылезет за экран. Высота
// 420px в .map совпадает с предложенной.
//
// Пустая строка полностью выключает карту: на её месте останется ссылка наружу.
export const MAP_EMBED_SRC: string =
  'https://yandex.ru/map-widget/v1/?um=constructor%3A900819479a769c005a23ccfb68b3ff21cab59dd467f8b9e1013208166a1e3982&source=constructor';
